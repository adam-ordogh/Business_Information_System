<Application x:Class="SiBerlo.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SiBerlo.ViewModels"
             xmlns:v="clr-namespace:SiBerlo.Views"
             xmlns:conv="clr-namespace:SiBerlo.Converters"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             StartupUri="Views/MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- 1. Külső erőforrások -->
            <ResourceDictionary.MergedDictionaries>
                <!-- Material Design erőforrások -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml"/>

                <!-- Saját színek külön dictionary-ben -->
                <ResourceDictionary>
                    <!-- Alapszínek -->
                    <SolidColorBrush x:Key="SidebarForeground" Color="#444444"/>
                    <SolidColorBrush x:Key="SidebarBackground" Color="#8fce00"/> 
                    <SolidColorBrush x:Key="SidebarHover" Color="#80b900"/>
                    <SolidColorBrush x:Key="SidebarSelected" Color="#649000"/>
                    <!--<SolidColorBrush x:Key="SidebarBackground" Color="#FF424242"/>-->
                    <!--<SolidColorBrush x:Key="SidebarForeground" Color="White"/>-->
                    <!--<SolidColorBrush x:Key="SidebarHover" Color="#3D3D3D"/>-->
                    <!--<SolidColorBrush x:Key="SidebarSelected" Color="#303030"/>-->
                    
                    <!-- Tartalom színek -->
                    <!--<SolidColorBrush x:Key="MaterialDesignPaper" Color="White"/>
                    <SolidColorBrush x:Key="MaterialDesignPaperHover" Color="Gainsboro"/>
                    <SolidColorBrush x:Key="MaterialDesignBody" Color="Black"/>-->
                    <SolidColorBrush x:Key="MaterialDesignPaper" Color="White"/>
                    <SolidColorBrush x:Key="MaterialDesignPaperHover" Color="Gainsboro"/>
                    <SolidColorBrush x:Key="MaterialDesignBody" Color="Black"/>
                    
                    
                    <SolidColorBrush x:Key="MaterialDesignGridVorder" Color="Green"/>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>

            <!-- 2. Konverterek -->
            <conv:BoolToIconConverter x:Key="BoolToIconConverter" />
            
            <!-- 3. DataTemplate-ek -->
            <DataTemplate DataType="{x:Type vm:AlkalmazottakViewModel}">
                <v:AlkalmazottakView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:UgyfelekViewModel}">
                <v:UgyfelekView/>
            </DataTemplate>

            <!-- 4. Stílusok -->
            <!-- 4.1 Sidebar stílusok -->
            <Style x:Key="MaterialSidebarButton" TargetType="Button">
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="Padding" Value="16 8"/>
                <Setter Property="Margin" Value="0 4"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="Foreground" Value="{StaticResource SidebarForeground}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="4">
                                <ContentPresenter VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource SidebarHover}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="CleanExpanderToggleButton" TargetType="ToggleButton">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource SidebarForeground}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid Background="Transparent">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Header content -->
                                <ContentPresenter Grid.Column="0"
                                      Content="{TemplateBinding Content}"
                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left"
                                      Margin="{TemplateBinding Padding}"/>

                                <!-- Chevron arrow icon -->
                                <Viewbox Grid.Column="1" 
                             Width="16" 
                             Height="16"
                             Margin="4 0 0 0"
                             VerticalAlignment="Center">
                                    <Path x:Name="Arrow"
                              Stroke="{TemplateBinding Foreground}"
                              StrokeThickness="2"
                              StrokeEndLineCap="Round"
                              StrokeStartLineCap="Round"
                              Fill="Transparent"
                              Data="M 4,6 L 8,10 L 12,6"/>
                                </Viewbox>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <!-- Rotates the chevron to point up when expanded -->
                                    <Setter TargetName="Arrow" Property="Data" Value="M 4,10 L 8,6 L 12,10"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource SidebarHover}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- MaterialSidebarExpanderStyle -->
            <Style x:Key="MaterialDesignExpander" TargetType="Expander">
                <Setter Property="Foreground" Value="{StaticResource SidebarForeground}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Margin" Value="0 4 0 0"/>
                <Setter Property="Padding" Value="0"/>
                <!-- Fontos: a padding itt 0 -->
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Expander">
                            <Border BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Background="Transparent"
                                    CornerRadius="4">
                                <DockPanel>
                                    <!-- A header teljes hover-ral -->
                                    <Border x:Name="HeaderBorder"
                                            DockPanel.Dock="Top"
                                            Background="Transparent"
                                            Padding="8">
                                        <ToggleButton x:Name="HeaderSite"
                                                        Style="{StaticResource CleanExpanderToggleButton}"
                                                        Content="{TemplateBinding Header}"
                                                        ContentTemplate="{TemplateBinding HeaderTemplate}"
                                                        Foreground="{TemplateBinding Foreground}"
                                                        IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                        Background="Transparent"/>
                                    </Border>

                                    <ContentPresenter x:Name="ExpandSite"
                                                        DockPanel.Dock="Bottom"
                                                        Visibility="Collapsed"/>
                                </DockPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsExpanded" Value="True">
                                    <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource SidebarSelected}"/>

                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource SidebarHover}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>                
            </Style>



            <!-- 4.2 Alapértelmezett kontroll stílusok -->
            <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignTextBox}">
                <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource MaterialDesignPaperHover}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignTextAreaBorder}"/>
                    </Trigger>
                    <Trigger Property="IsFocused" Value="True">
                        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryHueMidBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="DataGrid" BasedOn="{StaticResource MaterialDesignDataGrid}">
                <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}"/>
                <Setter Property="RowBackground" Value="Transparent"/>
                <Setter Property="AlternatingRowBackground" Value="{DynamicResource MaterialDesignGrid}"/>
            </Style>

            <Style TargetType="CheckBox" BasedOn="{StaticResource MaterialDesignCheckBox}">
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                <Setter Property="Margin" Value="0 0 0 8"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="materialDesign:CheckBoxAssist.CheckBoxSize" Value="20"/>
                <Style.Triggers>
                    <Trigger Property="IsChecked" Value="True">
                        <Setter Property="Background" Value="{DynamicResource MaterialDesignBody}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
    