<UserControl x:Class="SiBerlo.Views.UgyfelekView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SiBerlo.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Add Client Form -->
        <Border Grid.Row="0" 
                CornerRadius="4" 
                Padding="16" 
                Background="{DynamicResource MaterialDesignPaper}"
                Margin="0 0 0 16">
            <StackPanel>
                <TextBlock Text="{Binding FormTitle}" 
                           Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                           Margin="0 0 0 16"/>
                <TextBox materialDesign:HintAssist.Hint="Név" 
                         Text="{Binding NewCustomerName, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 0 0 8"/>
                <TextBox materialDesign:HintAssist.Hint="Cím" 
                         Text="{Binding NewCustomerAddress, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 0 0 8"/>
                <TextBox materialDesign:HintAssist.Hint="Telefonszám" 
                         Text="{Binding NewCustomerPhone, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 0 0 8"/>
                <TextBox materialDesign:HintAssist.Hint="Email" 
                         Text="{Binding NewCustomerEmail, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 0 0 16"/>
                <TextBox materialDesign:HintAssist.Hint="Kedvezmény %" 
                         Text="{Binding NewCustomerDiscount, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 0 0 16"/>
                <Button Content="Hozzáadás" 
                        Command="{Binding AddCustomerCommand}"
                        HorizontalAlignment="Right"
                        Style="{StaticResource MaterialDesignRaisedButton}"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="Frissítés"
                            Command="{Binding UpdateCustomerCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"/>
                    <Button Content="Törlés"
                            Command="{Binding DeleteCustomerCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="Tomato"/>
                    <Button Content="Mégse"
                            Command="{Binding CancelEditCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"/>
                </StackPanel>
            </StackPanel>
        </Border>
        <!-- Customer List -->
        <TextBlock Grid.Row="1" 
                   Text="Ügyfelek listája" 
                   Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                   Margin="0 0 0 8"/>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Customers}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectedItem="{Binding SelectedCustomer}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" />
                <DataGridTextColumn Header="Név" Binding="{Binding Nev}" />
                <DataGridTextColumn Header="Cím" Binding="{Binding Cim}" />
                <DataGridTextColumn Header="Telefonszám" Binding="{Binding Telefon}" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" />
                <DataGridTextColumn Header="Kedvezmény %" Binding="{Binding Kedvezmeny}" />
                <DataGridTextColumn Header="Regisztráció időpontja" Binding="{Binding RegisztracioDatuma}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
