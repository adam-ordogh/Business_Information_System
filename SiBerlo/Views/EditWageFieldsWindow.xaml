<Window x:Class="SiBerlo.Views.EditWageFieldsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:SiBerlo.Views"             
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="EditWageFieldsWindow" Height="270" Width="300">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Jutalék:"/>
            <TextBox Text="{Binding Jutalek}" Margin="0 0 0 10"/>
            <TextBlock Text="Egyéb pótlék:"/>
            <TextBox Text="{Binding EgyebPotlek}" Margin="0 0 0 10"/>
            <TextBlock Text="Előleg:"/>
            <TextBox Text="{Binding Eloleg}" Margin="0 0 0 10"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Mentés" Width="80" Margin="0 0 10 0" IsDefault="True" Click="Save_Click" Style="{StaticResource MaterialDesignRaisedButton}"/>
                <Button Content="Mégse" Width="80" IsCancel="True" Style="{StaticResource MaterialDesignRaisedButton}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
