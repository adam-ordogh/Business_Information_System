<UserControl x:Class="SiBerlo.Views.AlkalmazottakView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SiBerlo.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Add Employee Form -->
        <Border Grid.Row="0" 
                CornerRadius="4" 
                Padding="16" 
                Background="{DynamicResource MaterialDesignPaper}"
                Margin="0 0 0 16">
            <StackPanel>
                <TextBlock Text="{Binding FormTitle}" 
                           Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                           Margin="0 0 0 16"/>

                <TextBox materialDesign:HintAssist.Hint="Név" 
                         Text="{Binding NewEmployeeName, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 0 0 8"/>
                
                <ComboBox materialDesign:HintAssist.Hint="Típus"
                          ItemsSource="{Binding WorkPositionTypes}"
                          SelectedItem="{Binding NewEmployeePosition, UpdateSourceTrigger=PropertyChanged}"
                          Margin="0 0 0 8"/>

                <TextBox materialDesign:HintAssist.Hint="Alap fizetés" 
                         Text="{Binding NewEmployeeBaseSalary, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                         Margin="0 0 0 8"/>

                <TextBox materialDesign:HintAssist.Hint="Bónusz jutalék" 
                         Text="{Binding NewEmployeeBonusPercentage, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                         Margin="0 0 0 16"/>

                <CheckBox Content="Aktív" 
                          IsChecked="{Binding NewEmployeeActive}"
                          Margin="0 0 0 16"
                          materialDesign:CheckBoxAssist.CheckBoxSize="20"/>

                <Button Content="Hozzáadás" 
                        Command="{Binding AddEmployeeCommand}"
                        HorizontalAlignment="Right"
                        Style="{StaticResource MaterialDesignRaisedButton}"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="Frissítés"
                        Command="{Binding UpdateEmployeeCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}" />
                    <Button Content="Törlés"
                        Command="{Binding DeleteEmployeeCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="Tomato"
                        Foreground="White"/>
                    <Button Content="Mégse"
                            Command="{Binding CancelEditCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}" />
                </StackPanel>
            </StackPanel>
        </Border>
        <!-- Employee List -->
        <TextBlock Grid.Row="1" 
                   Text="Alkalmazottak listája" 
                   Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                   Margin="0 0 0 8"/>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Employees}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectedItem="{Binding SelectedEmployee}" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Név" Binding="{Binding Nev}"/>
                <DataGridTextColumn Header="Pozíció" Binding="{Binding Beosztas}"/>
                <DataGridTextColumn Header="Felvétel időpontja" Binding="{Binding BelepesDatuma}"/>
                <DataGridTextColumn Header="Alap fizetés" Binding="{Binding Alapber, StringFormat={}{0:N0}}"/>
                <DataGridTextColumn Header="Bónusz %" Binding="{Binding JutalekSzazalek, StringFormat={}{0}%}"/>
                <DataGridTemplateColumn Header="Aktív">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <materialDesign:PackIcon 
                                Width="20" Height="20"
                                Kind="{Binding Aktiv, Converter={StaticResource BoolToIconConverter}}"
                                Foreground="Gray"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>